# 핵심 의존성 (필수)
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
requests>=2.25.0,<3.0.0
python-dotenv>=0.19.0,<1.0.0

# 데이터 처리 및 분석
scipy>=1.7.0,<2.0.0
scikit-learn>=1.0.0,<2.0.0

# API 및 HTTP
urllib3>=1.26.0,<2.0.0
certifi>=2021.5.25

# 로깅 및 모니터링
psutil>=5.8.0,<6.0.0

# 테스트 (개발 환경)
pytest>=6.2.0,<8.0.0
pytest-cov>=2.12.0,<5.0.0
pytest-mock>=3.6.0,<4.0.0

# 코드 품질 (개발 환경)
black>=21.0.0,<24.0.0
flake8>=3.9.0,<7.0.0
mypy>=0.910,<2.0.0

# 문서화 (개발 환경)
sphinx>=4.0.0,<8.0.0
sphinx-rtd-theme>=0.5.0,<3.0.0

# 선택적 의존성 (성능 향상)
# numba>=0.53.0,<1.0.0  # JIT 컴파일 (선택사항)
# cython>=0.29.0,<4.0.0  # C 확장 (선택사항)

# 선택적 의존성 (시각화)
# matplotlib>=3.3.0,<4.0.0  # 차트 생성 (선택사항)
# plotly>=5.0.0,<6.0.0  # 인터랙티브 차트 (선택사항)

# 선택적 의존성 (데이터베이스)
# sqlalchemy>=1.4.0,<3.0.0  # ORM (선택사항)
# sqlite3  # 내장 (Python 표준 라이브러리)

# 선택적 의존성 (캐싱)
# redis>=3.5.0,<5.0.0  # Redis 캐시 (선택사항)
# memcached>=1.59.0,<2.0.0  # Memcached (선택사항)

# 선택적 의존성 (병렬 처리)
# joblib>=1.0.0,<2.0.0  # 병렬 처리 (선택사항)
# dask>=2021.5.0,<2024.0.0  # 분산 처리 (선택사항)

# 선택적 의존성 (머신러닝)
# xgboost>=1.5.0,<3.0.0  # XGBoost (선택사항)
# lightgbm>=3.2.0,<5.0.0  # LightGBM (선택사항)

# 선택적 의존성 (웹 프레임워크)
# fastapi>=0.68.0,<1.0.0  # API 서버 (선택사항)
# uvicorn>=0.15.0,<1.0.0  # ASGI 서버 (선택사항)

# 선택적 의존성 (모니터링)
# prometheus-client>=0.11.0,<1.0.0  # 메트릭 수집 (선택사항)
# grafana-api>=1.0.0,<2.0.0  # 대시보드 (선택사항)














# MCP KIS Integration v2.6 - 의존성

# === 필수 의존성 ===
requests>=2.28.0  # HTTP 클라이언트

# === 선택적 의존성 (권장) ===

# 멀티프로세스 안전성 (토큰 캐시 파일 락)
filelock>=3.12.0

# 종목명 검색 (KOSPI 마스터 파일)
pandas>=1.5.0
openpyxl>=3.0.0  # Excel 파일 읽기

# 한국 시간대 (Python < 3.9)
# Python >= 3.9는 zoneinfo 내장
tzdata>=2023.3; python_version < "3.9"

# === 참고 ===
# filelock 없이도 동작 (스레드 락 폴백)
# pandas 없이도 동작 (API 폴백)


